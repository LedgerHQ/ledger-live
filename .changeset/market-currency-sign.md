---
"@ledgerhq/live-common": patch
---

Market/price values now display the Ledger fiat sign (e.g. `$`, `CA$`) instead of the locale-dependent currency code/symbol (e.g. `US$`, or `$` for CAD), aligning Market market cap and volume with the displayed price and balances.
