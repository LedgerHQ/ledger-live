---
"@devtools/pay-card": minor
"@devtools/bindings": minor
---

Add an "Env vars" section to the Card / Pay DevTool. It shows the value the app reads for CARD_BAANX_API_URL and CARD_BAANX_CLIENT_KEY, and it sets either one from an input. Each input starts on the Baanx development tenant, so a tester switches with one press.
