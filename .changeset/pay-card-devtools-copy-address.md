---
"@devtools/pay-card": patch
---

Copy a joined wallet's address from the Card balance devtool.

- Every "Card linked combined wallets" row gets a "Copy address" button, so a top-up address is taken rather than transcribed off the screen.
- Mobile only: the desktop tool is untouched.
