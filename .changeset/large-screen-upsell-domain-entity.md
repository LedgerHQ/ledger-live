---
"@domain/entity-large-screen-upsell-modal": patch
---

Create the large-screen upsell modal domain entity package: `{ retries, lastSeenAt }` schema, Redux slice, and selectors, ported 1:1 (same shape, same action names) from `@ledgerhq/live-engagement`'s mobile slice.
