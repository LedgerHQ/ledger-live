---
"@devtools/shell": minor
---
Hook `devtools/*` into the SonarQube pipeline: coverage scripts, jest-sonar reporter, Nx scope tag, and a standalone `test-devtools` reusable workflow — devtools coverage now flows into every PR scan and nightly report.