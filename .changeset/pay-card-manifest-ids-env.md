---
"ledger-live-desktop": patch
"@shared/env": patch
"@shared/feature-flags": minor
---

feat(pay-card): read the Baanx login and hosted manifest ids from the env, not from the `lwdPayTab` flag params
