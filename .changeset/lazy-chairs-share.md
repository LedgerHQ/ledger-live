---
"ledger-live-desktop": minor
---

Enhance the shared `LineChart` component with high/low value point markers, a hover scrubber showing a formatted date/price tooltip, and configurable area fill (`showArea`) and X/Y axes (`showXAxis`/`showYAxis` plus passthrough `xAxis`/`yAxis` config), and apply them to the AssetDetail chart.
