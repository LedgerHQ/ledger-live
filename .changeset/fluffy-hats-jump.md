---
"@ledgerhq/dummy-wallet-app": patch
---

Add a self-signed-HTTPS dev mode (`start:https`) plus a matching manifest, to test the mobile WebView's `allowsUnsecureHttps` patch against a real self-signed certificate
