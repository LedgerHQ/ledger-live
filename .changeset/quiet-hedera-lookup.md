---
"@ledgerhq/ledger-wallet-framework": minor
"@ledgerhq/live-common": minor
---

feat(framework): optional `addressLookup` bridge hook for network-assigned addresses + wire Hedera's lookup and local coin module API
