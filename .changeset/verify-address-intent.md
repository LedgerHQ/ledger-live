---
"@ledgerhq/live-common": minor
---

Add verifyAddressIntent (DIE): a device intent that verifies a receive address on the device's Secure Screen via DmkCompatTransport + getAddress verify, emitting pending/verified/cancelled/unsupported states
