---
"live-mobile": patch
---

Add an optional native binding to the Zcash Rust engine (iOS/Android C ABI), exercised by a development-only startup probe. Off unless the prebuilt artifacts are fetched; nothing in the wallet consumes it yet
