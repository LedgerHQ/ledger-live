---
"@features/flow-pay-card-details": minor
---

Align the mobile card visual with the Figma design: `body3` balance label above a `sm` amount, a new opt-in `CardFade` that washes the bottom of the card into the page background, and no more stroke on the card face.
