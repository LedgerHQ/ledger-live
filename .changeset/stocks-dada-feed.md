---
"@ledgerhq/live-common": minor
---

Add `useStocksData` hook exposing a paginated DADA stocks feed (`/assets?categories=stocks`) with merged full rows, mirroring `useAssetsData` but isolated from it
