---
"@domain/api-card-management": patch
"live-mobile": patch
---

Answer the mocked Pay Card wallet reorder with the order alone, so the linked assets keep the amounts they were showing, and hold that answer open long enough for the reordered row to show its spinner.
