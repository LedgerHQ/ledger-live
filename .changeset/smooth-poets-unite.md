---
"live-mobile": patch
---

AssetDetail: support token currencies — replace `findCryptoCurrencyById` with `useCurrencyById`, move coin capsule setup to screen via `useLayoutEffect`, and propagate `CryptoOrTokenCurrency` types with a shared `AssetDetailCurrencyProps`
