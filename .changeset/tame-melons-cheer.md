---
"@ledgerhq/live-common": patch
---

Update @ledgerhq/coin-tezos to 12.0.1 and migrate the tezos family to its locked-down public API (`api`, `types`, `config`, `logic-public`, `network`, `constants`); the staking position predicates (isDelegationPosition/isStakePosition/isUnstakingPosition/isFinalizablePosition) are now owned by Ledger Live
