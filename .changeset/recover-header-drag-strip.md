---
"ledger-live-desktop": patch
---

Fix Recover webview top gap and make header buttons clickable on macOS by removing the legacy margin workaround and shortening the AppRegionDrag strip on `/recover` routes
