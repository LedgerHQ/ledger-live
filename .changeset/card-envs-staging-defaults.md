---
"@shared/env": patch
---

Set the default of `CARD_API_URL` to `https://dev.api.baanx.com` and the default of `CARD_BAANX_CLIENT_KEY` to the Baanx staging client key. The Card feature now points at the staging tenant without a manual override.
