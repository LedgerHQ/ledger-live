---
"live-mobile": minor
---

Enhance the shared mobile `LineChart` with high/low value point markers, a touch scrubber showing a formatted date/price tooltip, and configurable area fill (`showArea`) and X/Y axes (`showXAxis`/`showYAxis` plus passthrough `xAxis`/`yAxis` config), and apply them to the AssetDetail balance chart.
