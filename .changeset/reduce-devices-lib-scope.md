---
"@ledgerhq/devices": minor
---

Reduce the scope of `@ledgerhq/devices` to the devices list only. The transport framing helper subpath exports (`hid-framing`, `ble/sendAPDU`, `ble/receiveAPDU`) are removed.
