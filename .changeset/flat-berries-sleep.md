---
"@ledgerhq/types-live": minor
"@ledgerhq/live-common": minor
---

Declare `lldTezosStaking` and `llmTezosStaking` feature flags (disabled by default) gating Tezos Paris-upgrade staking UI on desktop and mobile respectively
