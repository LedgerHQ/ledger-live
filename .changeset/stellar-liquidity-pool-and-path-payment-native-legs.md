---
"@ledgerhq/coin-stellar": minor
---

Surface the native XLM legs of `path_payment_strict_send`, `path_payment_strict_receive`, `liquidity_pool_deposit` and `liquidity_pool_withdraw` operations in `listOperations` so the operations history reconciles with the on-chain account balance.
