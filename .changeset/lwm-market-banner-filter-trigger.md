---
"live-mobile": minor
---

Add the market banner filter trigger: a `MediaButton` selector in the banner subheader showing the active filter label and opening the filter selection drawer on press. Gated behind `assetDiscoverability`. Also renames the banner title to "Market".
