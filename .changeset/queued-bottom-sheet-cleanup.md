---
"live-mobile": patch
---

Cleanup the drawer stack: remove the legacy `QueuedDrawerGorhom` fallback (the Lumen bottom sheet is now always used) and rename `QueuedDrawerBottomSheet` to `QueuedBottomSheet`.
