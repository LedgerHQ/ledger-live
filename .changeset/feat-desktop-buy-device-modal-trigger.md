---
"ledger-live-desktop": minor
---

Use HOOKS_TRACKING_LOCATIONS for Buy Device modal trigger (origin flow). Native flows (Manager, Send, Receive, Add account, Stake, Ledger Sync) now set origin flow via the shared enum so the modal shows the correct trigger in analytics.
