---
"@devtools/feature-flags": minor
---

Add a sidebar that triggers when clicking on a flag. It contains a switch, a button to restore a flag if it has been overriden and a jsonEditor to change the parameters of the flag. There is also a diffView to see the difference between the jsonEditor content and the current flag parameters.
