---
"ledger-live-desktop": minor
---

feat(tezos): add Unstake flow modal (LIVE-29537) — registers `MODAL_TEZOS_UNSTAKE` with amount/device/confirmation steps and a single `useBridgeTransaction` in `mode: "unstake"`. No entry-point wired yet.
