---
"ledger-live-desktop": minor
---

Adapt transaction history icons to reflect aggregated assets mode: show the parent currency icon for tokens when aggregation is enabled, otherwise keep the token icon with its network badge
