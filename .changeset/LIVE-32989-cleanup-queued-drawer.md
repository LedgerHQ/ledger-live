---
"live-mobile": patch
---

Remove the legacy Gorhom `QueuedDrawer` implementation (migrating the last consumer, the Cardano DRep delegation drawer, to the Lumen bottom sheet) and rename `QueuedDrawerBottomSheet` to `QueuedBottomSheet`.
