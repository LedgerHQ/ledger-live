---
"@ledgerhq/coin-filecoin": minor
---

feat(coin-filecoin): add CoinModuleApi (Alpaca) support for native FIL and FEVM ERC-20 tokens via `createApi` factory. Legacy `createBridges` export unchanged.
