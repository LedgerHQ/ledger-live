---
"@shared/analytics": minor
"@shared/analytics-react": minor
---

Export `publishAnalyticsEvent`, align `LoggableEvent` property names with existing analytics, and re-track `<TrackPage>` when page properties change.
