---
"@devtools/registry": patch
"@ledgerhq/generators": minor
---

Add Nx generator to scaffold and auto-wire new devtool packages. Extracts the registry barrel into a dedicated `registry.ts` file, re-generated on each run.
