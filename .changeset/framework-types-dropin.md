---
"@ledgerhq/ledger-wallet-framework": minor
---

Add `CryptoOrTokenCurrency` type alias to `/types` export, completing the structural parity with `@ledgerhq/types-cryptoassets` for the currency type surface (`CryptoCurrency`, `TokenCurrency`, `FiatCurrency`, `Unit`, `ExplorerView`, `CryptoOrTokenCurrency`, `Currency`, `CryptoCurrencyId`, `LedgerExplorerId`).
