---
"ledger-live-desktop": minor
"live-mobile": minor
"@card/core": minor
---

Bootstrap the Card platform — a lightweight component host embedded in the Pay tab of Desktop and Mobile. Add `@card/core` (the `<Card />` renderer plus its static component registry and playground template); wire both host apps to render `<Card />` in their Pay tab.
