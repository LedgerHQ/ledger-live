amends "package://github.com/jdx/hk/releases/download/v1.40.0/hk@1.40.0#/Config.pkl"

hooks {
    ["pre-commit"] {
        steps {
            ["gitleaks"] {
                check = "gitleaks git --pre-commit --staged --redact --no-banner --verbose"
            }
        }
    }
}
