snapshot: ledger-live-desktop-agent-cde
lint: |
  echo "Running lint..."
  time pnpm lint --tui false
  echo "Building ledger-live-desktop..."
  time pnpm build:lld --tui false
  echo "Done"
env:
  IS_KODA: "1"
