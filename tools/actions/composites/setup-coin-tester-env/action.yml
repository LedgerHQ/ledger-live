name: "Setup Env for Coin Tester"
description: "Installs deps, builds modules"
runs:
  using: "composite"
  steps:
    - run: pnpm i --filter="!./apps/**"
      shell: bash

    - run: pnpm turbo build --filter="@ledgerhq/coin-tester-*"
      shell: bash
