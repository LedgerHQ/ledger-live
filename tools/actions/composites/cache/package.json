{
  "name": "@ledgerhq/actions-cache",
  "version": "0.0.0",
  "private": true,
  "description": "S3 cache actions backed by s5cmd and pzstd; runs as TypeScript with no build step.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint ./",
    "lint:fix": "oxlint ./ --fix --quiet"
  }
}
