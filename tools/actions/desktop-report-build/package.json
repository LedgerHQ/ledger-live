{
  "name": "@actions/desktop-report-build",
  "version": "1.0.1",
  "description": "Report the results of desktop builds in a `summary.json` file",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsup -d build main.ts",
    "lint": "eslint ./ --cache",
    "lint:fix": "pnpm lint --fix"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "node-fetch": "^3.3.2",
    "tsup": "^7.0.0"
  }
}
