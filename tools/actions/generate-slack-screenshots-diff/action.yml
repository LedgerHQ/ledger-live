name: generate-slack-screenshots-diff
description: prepare diff comment for slack
inputs:
  workspace:
    description: usually github.workspace
    required: true
  images:
    description: path to images.json
    required: true

runs:
  using: "node12"
  main: "build/index.js"
