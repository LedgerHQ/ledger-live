{
  "name": "@actions/upload-images",
  "version": "1.0.2",
  "description": "Uploads images found in a folder to s3 and creates a json file for later reference",
  "author": "Valentin D. Pinkman <valentin.d.pinkman@icloud.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsup -d build main.ts",
    "lint": "eslint ./ --cache",
    "lint:fix": "pnpm lint --fix"
  },
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@aws-sdk/client-s3": "^3.370.0",
    "tsup": "^7.0.0"
  }
}
