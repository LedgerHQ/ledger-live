# Jest Allure2 reporter

> [!CAUTION]
> **Status: UNSTABLE** — Vendored E2E tooling maintained by the QAA team.

This private package vendors the runtime source of
[`jest-allure2-reporter@2.2.8`](https://github.com/wix-incubator/jest-allure2-reporter/tree/v2.2.8).
It is loaded directly from TypeScript by small CommonJS entrypoints, so no generated `dist` is
committed.

The upstream source is MIT licensed. See `LICENSE.upstream`.
