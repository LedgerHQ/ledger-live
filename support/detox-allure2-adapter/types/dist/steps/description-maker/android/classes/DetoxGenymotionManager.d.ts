import { StepDescription } from '../../types';
export declare class DetoxGenymotionManager {
    static setLocation(lat: number, lon: number): StepDescription;
    static getGenymotionManager(): StepDescription;
}
