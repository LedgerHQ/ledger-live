import type { StepDescription } from '../../types';
import type { Predicate } from '../detox-payload';
export declare function formatPredicate(predicate?: Predicate, prefix?: string): StepDescription;
