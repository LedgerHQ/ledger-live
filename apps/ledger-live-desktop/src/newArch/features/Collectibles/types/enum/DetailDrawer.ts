export enum FieldStatus {
  Loading = "loading",
  Loaded = "loaded",
  Error = "error",
  NoData = "nodata",
  Queued = "queued",
}

export enum ItemType {
  Separator = "separator",
  ExternalLink = "external",
}
