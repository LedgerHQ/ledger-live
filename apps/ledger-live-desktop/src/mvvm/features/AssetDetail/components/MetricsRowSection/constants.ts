/** Shared layout for PnL + Earn metric cards in the asset detail metrics row. */
export const METRICS_ROW_CLASS_NAME = "flex flex-wrap items-stretch gap-12";

/** Cards grow equally but do not shrink below a readable label width; row wraps when needed. */
export const METRICS_ROW_CARD_CLASS_NAME = "min-w-[12rem] flex-1";
