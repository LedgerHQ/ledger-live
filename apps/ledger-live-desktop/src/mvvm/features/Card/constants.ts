/** Max height of the card wallet image in pixels */
export const CARD_IMAGE_MAX_HEIGHT_PX = 430;

/** Max width of the card wallet image in pixels */
export const CARD_IMAGE_MAX_WIDTH_PX = 415;

/** Viewport height ratio cap for the image (e.g. 0.45 = 45vh) */
export const CARD_IMAGE_HEIGHT_VIEWPORT_RATIO = 0.45;

/** Viewport width ratio cap for the image (e.g. 0.45 = 45vw) */
export const CARD_IMAGE_WIDTH_VIEWPORT_RATIO = 0.45;

/** Max width of the subtitle text block in pixels */
export const CARD_SUBTITLE_MAX_WIDTH_PX = 330;
export const CARD_TRACKING_PAGE_NAME = "Card";
