import styled from "styled-components";
import { VerticalTimeline } from "@ledgerhq/react-ui";

const StepText = styled(VerticalTimeline.BodyText)`
  white-space: pre-wrap;
`;

export default StepText;
