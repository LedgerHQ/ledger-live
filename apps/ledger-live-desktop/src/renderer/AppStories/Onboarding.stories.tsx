import Default from "../Default";
import { MockProviderDecorator } from "./MockProviderDecorator";

export default {
  component: Default,
  title: "App/Onboarding",
  tags: ["!autodocs"],
  decorators: [MockProviderDecorator],
};

export const Onboarding = {};
