import React from "react";
import type { StepProps } from "~/renderer/modals/Send/types";

export const StepMandatoryPrivateSync = (_props: StepProps) => {
  return <p>simulating private sync...</p>;
};
