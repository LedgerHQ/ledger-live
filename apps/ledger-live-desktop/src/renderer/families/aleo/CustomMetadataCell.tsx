import React from "react";
import { AleoFamily } from "./types";

const MyCustomMetadataCell: AleoFamily["CustomMetadataCell"] = ({ operation }) => {
  return <div>CUSTOM</div>;
};

export default MyCustomMetadataCell;
