{
  "name": "ledger-live-desktop",
  "targets": {
    "build": {
      "inputs": [
        "default",
        "desktopEnv",
        "sharedEnv"
      ]
    },
    "build:testing": {
      "dependsOn": [
        "^build"
      ]
    },
    "lint": {
      "inputs": [
        "default",
        {
          "env": "CI_OS"
        }
      ]
    },
    "test": {
      "dependsOn": [
        "build:testing"
      ],
      "inputs": [
        "default",
        {
          "env": "CI_OS"
        }
      ]
    }
  }
}