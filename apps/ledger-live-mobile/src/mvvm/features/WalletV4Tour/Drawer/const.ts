export const PAGE_TRACKING_WALLET_V4_TOUR = "Product Tour WV4";

/** Fixed height of the swipeable slides area (Lottie animation + 2-line title + description). */
export const SLIDES_LIST_HEIGHT = 420;

/** Height of the page-dots progress indicator row, including its top/bottom margins. */
const PROGRESS_HEIGHT = 80;

/** Height of the footer's single `lg` button (the "explore" button is an absolute overlay). */
const FOOTER_HEIGHT = 64;

/**
 * Explicit height for the whole `Slides` block (list + progress + footer).
 */
export const SLIDES_CONTAINER_HEIGHT = SLIDES_LIST_HEIGHT + PROGRESS_HEIGHT + FOOTER_HEIGHT;
