export const CONTENT_AREA_HEIGHT = 208;

// Floor so the banner can't shrink the hero enough to crowd the title against the CTAs.
export const MIN_CONTENT_AREA_HEIGHT = 160;

// Top gap so the banner clears the floating Wallet 4.0 TopBar.
export const OS_UPDATE_BANNER_TOP_GAP = 40;
