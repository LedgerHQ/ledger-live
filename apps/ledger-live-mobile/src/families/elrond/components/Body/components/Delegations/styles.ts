import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  illustration: {
    alignSelf: "center",
    marginBottom: 16,
  },
  wrapper: {
    marginBottom: 16,
  },
});

export default styles;
