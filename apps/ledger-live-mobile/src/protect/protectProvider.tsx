import React, { ReactNode, useEffect, useState, useCallback } from "react";

type Props = {
  children: ReactNode;
};

function ProtectProvider(props: Props) {
  const { children } = props;

  return (<>
    
  </>);
}

export default ProtectProvider;
