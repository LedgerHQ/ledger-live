{
  "framework": null,
  "installCommand": "pnpm i -F \"web-tools...\" -F \"ledger-live\"",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=\"@ledgerhq/web-tools\"",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
