// Registers the live-common network interceptor and the WebHID transport module
// so getSdk(mock: false, ..., withDevice) works in the browser.
import "../live-common-setup";
import "../trustchain/live-common-setup";
