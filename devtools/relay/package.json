{
  "name": "@devtools/relay",
  "version": "0.4.0",
  "private": true,
  "description": "DevTools relay hub — localhost-only WebSocket forwarder (dev-only, never shipped)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "jest --config jest.config.js",
    "coverage": "jest --config jest.config.js --coverage",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@devtools/transport": "workspace:*",
    "qrcode-terminal": "^0.12.0",
    "ws": "catalog:"
  },
  "devDependencies": {
    "@swc/core": "catalog:",
    "@devtools/protocols": "workspace:*",
    "@swc/jest": "catalog:",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "@types/qrcode-terminal": "^0.12.0",
    "@types/ws": "catalog:",
    "jest": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
