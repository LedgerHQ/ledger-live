// Layer config. oxfmt finds it by walking up from any package below, which is also how an
// editor integration resolves settings. The content lives in the support/ preset.
export { default } from "@support/fmt-base/oxfmt.config";
