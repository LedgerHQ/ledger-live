{
  "name": "@devtools/transport",
  "version": "0.6.0",
  "private": true,
  "description": "Generic WebSocket transport for DevTools \u2014 connection, status, history, framing",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "format": "fmt-base src jest.config.js",
    "format:check": "fmt-base src jest.config.js --check",
    "typecheck": "tsc --noEmit",
    "test": "jest --config jest.config.js",
    "coverage": "jest --config jest.config.js --coverage",
    "lint": "lint-devtools src",
    "unimported": "pnpm knip --directory ../.. -W devtools/transport",
    "lint:fix": "lint-devtools src --fix"
  },
  "devDependencies": {
    "@ledgerhq/test-quarantine": "workspace:*",
    "@swc/core": "catalog:",
    "@swc/jest": "catalog:",
    "@types/jest": "catalog:",
    "jest": "catalog:",
    "typescript": "catalog:",
    "@support/fmt-base": "workspace:*",
    "@support/lint-devtools": "workspace:*",
    "@support/ts-devtools": "workspace:*"
  }
}
