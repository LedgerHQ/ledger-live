/* @flow */
import React from "react";
import { Text } from "react-native";

const el = (
  <Text
    style={{
      padding: 60,
      opacity: 0.5,
      textAlign: "center",
    }}
  >
    No Operations.
  </Text>
);

export default () => el;
