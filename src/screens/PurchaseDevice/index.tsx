import React, { memo } from "react";
import { Text } from "@ledgerhq/native-ui";
import { View } from "react-native";

const PurchaseDevice = () => (
  <View>
    <Text>Purchase Device Screen</Text>
  </View>
);

export default memo(PurchaseDevice);
