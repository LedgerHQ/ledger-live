/**
 * Version of the privacy policy users consented to for analytics / tracking (Segment).
 * Bump when the policy changes in a way that requires re-acknowledgment
 * (stored in settings as `analyticsConsentInfo.privacyPolicyVersion` or equivalent).
 */
export const CURRENT_PRIVACY_POLICY_VERSION = 1;
