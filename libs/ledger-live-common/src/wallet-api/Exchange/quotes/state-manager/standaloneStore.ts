/**
 * Moved to `@domain/api-swap-quotes/store.standalone`. Re-exported here so
 * wallet-cli's bootstrap keeps working.
 */
export { setupStandaloneSwapQuotesStore } from "@domain/api-swap-quotes/store.standalone";
