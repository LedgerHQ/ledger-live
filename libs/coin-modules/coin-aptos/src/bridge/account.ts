import { log } from "@ledgerhq/logs";

export async function sync(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function receive(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function createTransaction(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function updateTransaction(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function getTransactionStatus(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function estimateMaxSpendable(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function signOperation(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}

export async function broadcast(signature: string): Promise<string> {
  log("info", "signature", signature);

  return "";
}
