import { CryptoCurrency } from "@ledgerhq/types-cryptoassets";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function preload(currency: CryptoCurrency): Promise<void> {
  // TODO: Add me
}
