// MOCKED INTEGRATION TEST, REPLACE WITH PROPER IMPLEMENTATION LATER

describe("mocked test", () => {
  it("should always return true value", async () => {
    const result = true;
    expect(result).toEqual(true);
  });
});
