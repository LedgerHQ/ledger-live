/** Host the MSW handlers intercept, and the node URL the test coin config points at. */
export const TEST_VECHAIN_ENDPOINT = "https://vechain-test.ledger.com";

/** Production Thor endpoint, used by the suites that really hit the network. */
export const VECHAIN_ENDPOINT = "https://vechain.coin.ledger.com";
