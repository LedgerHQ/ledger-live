describe("EVM Family", () => {
  describe("api/etherscan.ts", () => {
    describe("fetchWithRetries", () => {
      it.todo("should retry on fail");
      it.todo("should throw after too many retries");
    });
  });
});

// To remove once we have an import of any kind, otherwise this can't be compiled under the '--isolatedModules' instruction
export {};
