version: '2.4'

services:
  speculos:
    image: ghcr.io/ledgerhq/speculos:sha-69bf753
    platform: linux/amd64
    volumes:
      - /Users/landry.monga/Github/ledger/ledger-live/libs/coin-modules/coin-evm/tmp:/speculos/apps
    env_file:
      - .env
    ports:
      - "${API_PORT}:${API_PORT}"
    build:
      context: /speculos
    command: --model nanos ./apps/app.elf --seed="${SEED}" --display headless --api-port ${API_PORT}
