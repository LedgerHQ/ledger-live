coin = btc_regtest

common = {
  atlas {
    enableMetrics = false
    enableTraces = false
  }

  node {
    http {
      uri  = "http://btcliked:18443"
      auth = "dXNlcjpwYXNz"
    }
    historicalParallelism = 2
    historicalBatchSize = 25
  }

  admin = {
    enabled = true
    server = {
      host = "0.0.0.0"
      port = 9877
    }
  }

  server = {
    host = "0.0.0.0"
    port = 9876
  }
}


eventProducing.zeromq.address = "tcp://btcliked:19999"
# NOTE: not sure about below
pollingMemPoolPeriod = 500ms // This is used only if the node.zeromq.address is not defined
