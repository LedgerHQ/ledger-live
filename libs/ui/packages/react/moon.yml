$schema: "https://moonrepo.dev/schemas/project.json"
language: javascript
type: library
tasks:
  build:
    outputs:
      - "lib/"
    options:
      mergeOutputs: replace
  typecheck:
    deps:
      - build
    inputs:
      - $CI_OS
    platform: node
