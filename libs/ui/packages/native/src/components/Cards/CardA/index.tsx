import React from "react";

type Props = {
  test?: string;
};

const CardA = (props: Props): React.ReactElement => {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  const { test } = props;

  return <></>;
};

export default CardA;
