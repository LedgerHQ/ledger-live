/// <reference types="react" />
type Props = {
    size?: number | string;
    color?: string;
};
declare function NOT({ size, color }: Props): JSX.Element;
declare namespace NOT {
    var DefaultColor: string;
}
export default NOT;
