# Rspack Example

This simple example demonstrates how to use `@ledgerhq/react-ui` with Rspack and React.

To run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
```

The entry point lives in `src/index.js` and renders the UI demo in `pages/index.js`.
