{
  "name": "@ledgerhq/next.js-example",
  "private": true,
  "version": "0.2.4",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env NODE_ENV=development rspack serve",
    "build": "cross-env NODE_ENV=production rspack build",
    "start": "cross-env NODE_ENV=production rspack serve",
    "serve": "http-server dist -p 8080",
    "lint": "eslint . --ext .js,.jsx",
    "clean:full": "npx rimraf dist node_modules"
  },
  "dependencies": {
    "@ledgerhq/react-ui": "workspace:^",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-is": "*",
    "styled-components": "^5.3.3"
  },
  "devDependencies": {
    "@rspack/cli": "catalog:",
    "@rspack/core": "catalog:",
    "@rspack/plugin-react-refresh": "catalog:",
    "cross-env": "^7.0.3",
    "http-server": "^14.0.0",
    "react-refresh": "0.14.0"
  }
}
