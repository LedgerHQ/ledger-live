diff --git a/dist/index.js b/dist/index.js
--- a/dist/index.js
+++ b/dist/index.js
@@ -90140,12 +90140,8 @@ async function createCLI(configOverride, runtimeDeps = {}) {
     });
   }
   let fullConfig = parsed.data;
-  const generatedPath = "./.bunli/commands.gen.ts";
-  try {
-    const resolvedPath = generatedPath.startsWith("./") ? new URL(generatedPath, `file://${process.cwd()}/`).href : generatedPath;
-    await import(resolvedPath);
-  } catch (error48) {
-    logger3.debug("Could not load generated types from %s: %O", generatedPath, error48);
-  }
+  // Dynamic import of .bunli/commands.gen.ts removed via pnpm patch.
+  // wallet-cli statically imports commands.gen before createCLI(), so this is redundant.
+  // In Bun standalone binaries the file:// import hangs.
   const commands = new Map;
   function getTerminalInfo() {
