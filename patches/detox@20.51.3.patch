diff --git a/local-cli/testCommand/TestRunnerCommand.js b/local-cli/testCommand/TestRunnerCommand.js
index 65e6cd70a6257138afd8270671f0ef1c555a9bbc..89c4f92166d331b142b71c5fbc511d8fc3ad5921 100644
--- a/local-cli/testCommand/TestRunnerCommand.js
+++ b/local-cli/testCommand/TestRunnerCommand.js
@@ -36,6 +36,7 @@ class TestRunnerCommand {
     this._startCommands = this._prepareStartCommands(commands, cliConfig);
     this._envFwd = {};
     this._terminating = false;
+    this._isRetry = false; // Track whether it's a retry
 
     if (runnerConfig.forwardEnv) {
       this._envFwd = this._buildEnvOverride(cliConfig, deviceConfig);
@@ -85,10 +86,12 @@ class TestRunnerCommand {
         }
 
         if (--runsLeft > 0) {
+          this._isRetry = true;
           // @ts-ignore
           detox.session.testSessionIndex++; // it is always the primary context, so we can update it
           this._noRetryArgs.forEach(arg => delete this._argv[arg]);
           this._argv._ = testFilesToRetry.map(useForwardSlashes);
+          this._setRetryTestNames(failedTestFiles, testFilesToRetry);
           this._logRelaunchError(testFilesToRetry);
         }
       }
@@ -109,6 +112,38 @@ class TestRunnerCommand {
       .value();
   }
 
+  /**
+   * Narrow the upcoming retry to the individual tests that failed, on top of the spec-file
+   * narrowing above. Consumed by e2e/mobile/jest.environment.ts, which prunes everything else
+   * out of the circus tree, so the tests that already passed are neither re-run nor re-reported.
+   *
+   * Passed by environment rather than as --testNamePattern on purpose: _buildSpawnArguments
+   * escapes only spaces before cp.spawn({ shell: true }), so a regex would reach /bin/sh with
+   * its metacharacters live. Exact names in a JSON array have no such hazard, and cannot
+   * mis-select the way an imperfectly escaped pattern can.
+   *
+   * A file that failed without producing assertion results (a module-load or transform error)
+   * yields no names. Narrowing is therefore all-or-nothing: if any retried file cannot supply
+   * names, every file is retried whole, which is the pre-existing behaviour.
+   *
+   * @param {{ testFilePath: string, isPermanentFailure?: boolean, failedTestNames?: string[] }[]} failedTestFiles
+   * @param {string[]} testFilesToRetry
+   */
+  _setRetryTestNames(failedTestFiles, testFilesToRetry) {
+    const namesByFile = new Map(failedTestFiles.map(r => [r.testFilePath, r.failedTestNames || []]));
+    const canNarrow = testFilesToRetry.every(f => (namesByFile.get(f) || []).length > 0);
+    const names = testFilesToRetry.flatMap(f => namesByFile.get(f) || []);
+
+    if (canNarrow) {
+      this._envFwd.E2E_RETRY_TEST_NAMES = JSON.stringify(names);
+      log.warn(`Retrying ${names.length} failed test(s) rather than the whole spec file(s).`);
+    } else {
+      // Leave no stale value from an earlier retry, or the next attempt prunes to the wrong set.
+      delete this._envFwd.E2E_RETRY_TEST_NAMES;
+      log.warn('Some failed spec files reported no test names; retrying them in full.');
+    }
+  }
+
   _prepareStartCommands(commands, cliConfig) {
     if (`${cliConfig.start}` === 'false') {
       return [];
@@ -210,11 +245,16 @@ class TestRunnerCommand {
     /* istanbul ignore next */
     const { _: specs = [], '--': passthrough = [], $0, ...argv } = this._argv;
     const { _: $0_, ...$0argv } = parser($0);
-
+    
+    // Remove shard arguments for retries
+    const filteredArgv = this._isRetry
+      ? _.omit(argv, ['shard'])
+      : argv;
+    
     return [
       ...$0_,
       ...unparse($0argv),
-      ...unparse(argv),
+      ...unparse(filteredArgv),
       ...unparse({ _: [...passthrough, ...specs] }),
     ].map(String);
   }
diff --git a/runners/jest/reporters/DetoxIPCReporter.js b/runners/jest/reporters/DetoxIPCReporter.js
index 9e4c6eaba0607d9ff427a55a3738056caccf512f..623bd4b346e443063f7fb68d9e67ee8f4b714f14 100644
--- a/runners/jest/reporters/DetoxIPCReporter.js
+++ b/runners/jest/reporters/DetoxIPCReporter.js
@@ -16,6 +16,10 @@ class DetoxIPCReporter {
       testFilePath: r.testFilePath,
       testExecError: r.testExecError,
       isPermanentFailure: lostTests > 0 || this._isPermanentFailure(r),
+      // Exact full names of the tests that failed, so a retry can re-run just those rather than
+      // the whole spec file. Empty when the suite died before running any test (testExecError),
+      // which the retry reads as "cannot narrow" and falls back to re-running the file.
+      failedTestNames: r.testResults.filter(t => t.status === 'failed').map(t => t.fullName),
     })));
   }
 
diff --git a/src/devices/allocation/DeviceAllocator.js b/src/devices/allocation/DeviceAllocator.js
index b615cd28cab5a5ec24f939ff12076394c53b091b..6c3c893267b566720a2483bbb78f8d93a70d69b2 100644
--- a/src/devices/allocation/DeviceAllocator.js
+++ b/src/devices/allocation/DeviceAllocator.js
@@ -38,10 +38,10 @@ class DeviceAllocator {
    * @param {Detox.DetoxDeviceConfig} deviceConfig
    * @returns {Promise<DeviceCookie>}
    */
-  async allocate(deviceConfig) {
+  async allocate(deviceConfig, workerPid) {
     const tid = this._counter++;
     return await log.trace.complete({ data: deviceConfig, id: tid }, 'allocate', async () => {
-      const cookie = await this._driver.allocate(deviceConfig);
+      const cookie = await this._driver.allocate(deviceConfig, workerPid);
       log.debug({ data: cookie }, `settled on ${cookie.name || cookie.id}`);
       this._ids.set(cookie.id, tid);
       return cookie;
diff --git a/src/devices/allocation/DeviceRegistry.js b/src/devices/allocation/DeviceRegistry.js
index 3741e0866f13cef09a6545295af6b9739b600391..f9c69b0bd4e793b78474b0fcf0479bc2d5bc69b7 100644
--- a/src/devices/allocation/DeviceRegistry.js
+++ b/src/devices/allocation/DeviceRegistry.js
@@ -55,16 +55,19 @@ class DeviceRegistry {
 
   /***
    * @param {string|Function} getDeviceId
+   * @param {number} [ownerPid] - Records this pid as the device's owner instead of the caller's
+   *   own pid. Allocation runs inside Detox's primary process, so without this every device
+   *   would be stamped with the ever-alive primary's pid, never able to reflect a dead worker.
    * @returns {Promise<string>}
    */
-  async registerDevice(getDeviceId) {
+  async registerDevice(getDeviceId, ownerPid) {
     return this._lockfile.exclusively(async () => {
       const deviceId = await safeAsync(getDeviceId);
       if (deviceId) {
         this._upsertDevice(deviceId, {
           busy: true,
           sessionId: this._sessionId,
-          pid: this._pidService.getPid(),
+          pid: ownerPid ?? this._pidService.getPid(),
         });
       }
       return deviceId;
@@ -127,6 +130,17 @@ class DeviceRegistry {
     });
   }
 
+  /**
+   * Whether a registered device's owning process has died. Caller must already hold
+   * this._lockfile exclusively - this does not acquire it itself.
+   * @param {string} udid
+   * @returns {boolean}
+   */
+  isDeviceZombie(udid) {
+    const device = [...this._getRegisteredDevices()].find(d => d.id === udid);
+    return !!device && !this._pidService.isAlive(device.pid);
+  }
+
   /**
    * @returns {Promise<DeviceList>}
    */
diff --git a/src/devices/allocation/drivers/ios/SimulatorAllocDriver.js b/src/devices/allocation/drivers/ios/SimulatorAllocDriver.js
index 38472ddad2940e659127869c3a1d0eb97889001c..9f953d05a2e0227085980165d94e9d9b73e0a8cb 100644
--- a/src/devices/allocation/drivers/ios/SimulatorAllocDriver.js
+++ b/src/devices/allocation/drivers/ios/SimulatorAllocDriver.js
@@ -36,14 +36,16 @@ class SimulatorAllocDriver {
 
   /**
    * @param deviceConfig { Object }
+   * @param workerPid { number } - the actual worker process using this device, so the registry
+   *   can detect it dying even though allocation itself runs in the primary process.
    * @return {Promise<IosSimulatorCookie>}
    */
-  async allocate(deviceConfig) {
+  async allocate(deviceConfig, workerPid) {
     const deviceQuery = new SimulatorQuery(deviceConfig.device);
 
     const udid = await this._deviceRegistry.registerDevice(async () => {
       return await this._findOrCreateDevice(deviceQuery);
-    });
+    }, workerPid);
 
     if (!udid) {
       throw new DetoxRuntimeError(`Failed to find device matching ${deviceQuery.getDeviceComment()}`);
@@ -121,21 +123,67 @@ class SimulatorAllocDriver {
    * @returns {Promise<String>}
    */
   async _findOrCreateDevice(deviceQuery) {
-    let udid;
-
     const { free, taken } = await this._groupDevicesByStatus(deviceQuery);
 
-    if (_.isEmpty(free)) {
-      const prototypeDevice = taken[0];
-      udid = this._applesimutils.create(prototypeDevice);
-      await this._runScreenshotWorkaround(udid);
-    } else {
-      udid = free[0].udid;
+    if (!_.isEmpty(free)) {
+      return free[0].udid;
     }
 
+    // CI only boots these devices by their fixed name and never recreates a missing one, so
+    // reclaim a dead worker's device instead of creating a replacement it can never delete.
+    const zombieUdid = await this._reclaimZombieForReuse(taken);
+    if (zombieUdid) {
+      return zombieUdid;
+    }
+    log.debug({}, `No reusable device among ${taken.length} taken; creating a new one`);
+
+    const prototypeDevice = taken[0];
+    const udid = this._applesimutils.create(prototypeDevice);
+    await this._runScreenshotWorkaround(udid);
     return udid;
   }
 
+  /**
+   * @param {Array<{udid: string}>} takenDevices
+   * @returns {Promise<string|undefined>}
+   * @private
+   */
+  async _reclaimZombieForReuse(takenDevices) {
+    for (const { udid } of takenDevices) {
+      if (!this._deviceRegistry.isDeviceZombie(udid)) continue;
+      if (await this._isDeviceSafeToReuse(udid)) {
+        return udid;
+      }
+    }
+    return undefined;
+  }
+
+  /**
+   * Called only once isDeviceZombie() confirms the owner is dead, so a lingering "Booted"
+   * state isn't a live owner - only isBooted() itself failing (e.g. the UDID no longer exists)
+   * is treated as unsafe.
+   * @param {string} udid
+   * @returns {Promise<boolean>}
+   * @private
+   */
+  async _isDeviceSafeToReuse(udid) {
+    try {
+      await this._applesimutils.shutdown(udid);
+    } catch (err) {
+      try {
+        await this._applesimutils.isBooted(udid);
+        log.debug({ err }, `Zombie simulator ${udid} would not shut down; reusing it anyway since its owning process is dead`);
+      } catch (verifyErr) {
+        log.warn(
+          { err: verifyErr },
+          `Could not verify zombie simulator ${udid}'s state after a failed shutdown; leaving it alone`,
+        );
+        return false;
+      }
+    }
+    return true;
+  }
+
   async _runScreenshotWorkaround(udid) {
     await this._applesimutils.takeScreenshot(udid, '/dev/null').catch(() => {
       log.debug({}, `
diff --git a/src/devices/common/drivers/android/exec/ADB.js b/src/devices/common/drivers/android/exec/ADB.js
index bde116c69467a8f16f1d420a8fdbac89443099f0..06ab2f0a482af02d8675cd4a85070a9e46a660d9 100644
--- a/src/devices/common/drivers/android/exec/ADB.js
+++ b/src/devices/common/drivers/android/exec/ADB.js
@@ -9,13 +9,15 @@ const { escape } = require('../../../../../utils/pipeCommands');
 const DeviceHandle = require('../tools/DeviceHandle');
 const EmulatorHandle = require('../tools/EmulatorHandle');
 
+const { DETOX_INSTALL_TIMEOUT = "60000" } = process.env;
+const DETOX_INSTALL_TIMEOUT_NUMBER = parseInt(DETOX_INSTALL_TIMEOUT, 10);
 const log = logger.child({ cat: 'device' });
 
 const DEFAULT_EXEC_OPTIONS = {
   retries: 1,
 };
 const DEFAULT_INSTALL_OPTIONS = {
-  timeout: 60000,
+  timeout: Number.isNaN(DETOX_INSTALL_TIMEOUT_NUMBER) ? 60000 : DETOX_INSTALL_TIMEOUT_NUMBER,
   retries: 3,
 };
 
@@ -380,7 +382,17 @@ class ADB {
   }
 
   async reverseRemove(deviceId, port) {
-    return this.adbCmd(deviceId, `reverse --remove tcp:${port}`);
+    try {
+      return await this.adbCmd(deviceId, `reverse --remove tcp:${port}`);
+    } catch (e) {
+      const stderr = e.stderr || '';
+      const message = e.message || '';
+      if (stderr.includes('not found') || message.includes('not found')) {
+        log.warn(`Skipping adb reverse --remove tcp:${port} on ${deviceId}: device not found`);
+        return;
+      }
+      throw e;
+    }
   }
 
   async emuKill(deviceId) {
diff --git a/src/ipc/IPCClient.js b/src/ipc/IPCClient.js
index bcc5384326c7fd7a640c2633de35461a2473301a..636f0a5a78bcdce0832b1bda0728d763fa35792d 100644
--- a/src/ipc/IPCClient.js
+++ b/src/ipc/IPCClient.js
@@ -60,8 +60,8 @@ class IPCClient {
     this._sessionState.patch(sessionState);
   }
 
-  async allocateDevice(deviceConfig) {
-    const { deviceCookie, error } = deserializeObjectWithError(await this._emit('allocateDevice', { deviceConfig }));
+  async allocateDevice(deviceConfig, workerPid) {
+    const { deviceCookie, error } = deserializeObjectWithError(await this._emit('allocateDevice', { deviceConfig, workerPid }));
     if (error) {
       throw error;
     }
diff --git a/src/ipc/IPCServer.js b/src/ipc/IPCServer.js
index 203ccd597a9908539fa987bf7a5fae45b0e8ecfd..a93d16347b45dc433301a2c9d4f97861c223328c 100644
--- a/src/ipc/IPCServer.js
+++ b/src/ipc/IPCServer.js
@@ -9,7 +9,7 @@ class IPCServer {
    * @param {import('./SessionState')} options.sessionState
    * @param {Detox.Logger} options.logger
    * @param {object} options.callbacks
-   * @param {(deviceConfig: DetoxInternals.RuntimeConfig['device']) => Promise<any>} options.callbacks.onAllocateDevice
+   * @param {(deviceConfig: DetoxInternals.RuntimeConfig['device'], workerPid?: number) => Promise<any>} options.callbacks.onAllocateDevice
    * @param {(cookie: any) => Promise<void>} options.callbacks.onDeallocateDevice
    */
   constructor({ sessionState, logger, callbacks }) {
@@ -104,11 +104,11 @@ class IPCServer {
     this._ipc.server.broadcast('sessionStateUpdate', newState);
   }
 
-  async onAllocateDevice({ deviceConfig }, socket) {
+  async onAllocateDevice({ deviceConfig, workerPid }, socket) {
     let deviceCookie;
 
     try {
-      deviceCookie = await this._callbacks.onAllocateDevice(deviceConfig);
+      deviceCookie = await this._callbacks.onAllocateDevice(deviceConfig, workerPid);
       this._ipc.server.emit(socket, 'allocateDeviceDone', { deviceCookie });
     } catch (error) {
       this._ipc.server.emit(socket, 'allocateDeviceDone', serializeObjectWithError({ error }));
diff --git a/src/realms/DetoxPrimaryContext.js b/src/realms/DetoxPrimaryContext.js
index e38fc192b151990116242196c47e6b26732f6dde..99d16b85f98673235108011db7049d88b474c48c 100644
--- a/src/realms/DetoxPrimaryContext.js
+++ b/src/realms/DetoxPrimaryContext.js
@@ -154,7 +154,7 @@ class DetoxPrimaryContext extends DetoxContext {
   }
 
   /** @override */
-  async [symbols.allocateDevice](deviceConfig) {
+  async [symbols.allocateDevice](deviceConfig, workerPid) {
     const deviceAllocator = await this[_createDeviceAllocator](deviceConfig);
 
     const retryOptions = {
@@ -165,12 +165,12 @@ class DetoxPrimaryContext extends DetoxContext {
     };
 
     return await retry(retryOptions, async () => {
-      return await this[_allocateDeviceOnce](deviceAllocator, deviceConfig);
+      return await this[_allocateDeviceOnce](deviceAllocator, deviceConfig, workerPid);
     });
   }
 
-  async [_allocateDeviceOnce](deviceAllocator, deviceConfig) {
-    const deviceCookie = await deviceAllocator.allocate(deviceConfig);
+  async [_allocateDeviceOnce](deviceAllocator, deviceConfig, workerPid) {
+    const deviceCookie = await deviceAllocator.allocate(deviceConfig, workerPid);
     this[_cookieAllocators][deviceCookie.id] = deviceAllocator;
 
     try {
diff --git a/src/realms/DetoxSecondaryContext.js b/src/realms/DetoxSecondaryContext.js
index e5795f1ef888e85e662503b1ea9bcc19a9adcae4..d3a0ee8aee219902bbb55014184a9cc8ddc758b8 100644
--- a/src/realms/DetoxSecondaryContext.js
+++ b/src/realms/DetoxSecondaryContext.js
@@ -65,7 +65,9 @@ class DetoxSecondaryContext extends DetoxContext {
   /** @override */
   async [symbols.allocateDevice](deviceConfig) {
     if (this[_ipcClient]) {
-      const deviceCookie = await this[_ipcClient].allocateDevice(deviceConfig);
+      // Device allocation always runs in the primary process, so its own pid is useless for
+      // detecting a crashed worker later - pass this worker's real pid to be recorded instead.
+      const deviceCookie = await this[_ipcClient].allocateDevice(deviceConfig, process.pid);
       return deviceCookie;
     } else {
       throw new DetoxInternalError('Detected an attempt to allocate a device using a non-initialized context.');
diff --git a/src/utils/ExclusiveLockfile.js b/src/utils/ExclusiveLockfile.js
index da29ae99c8b90877f15715cdeb692ed8a2b47569..ce884adcafa10e78b0b040dbbbe3c4eeaacd7452 100644
--- a/src/utils/ExclusiveLockfile.js
+++ b/src/utils/ExclusiveLockfile.js
@@ -6,6 +6,14 @@ const DetoxRuntimeError = require('../errors/DetoxRuntimeError');
 
 const retry = require('./retry');
 
+// Raise the lockfile stale threshold in EVERY environment, not just CI: Detox's 10_000ms default
+// causes ECOMPROMISED under heavy I/O. Override with DETOX_LOCKFILE_STALE_MS; default 60_000ms.
+const LOCKFILE_STALE_MS = parseInt(process.env.DETOX_LOCKFILE_STALE_MS, 10) || 60000;
+const LOCKFILE_OPTIONS = {
+  stale: LOCKFILE_STALE_MS,
+  update: Math.floor(LOCKFILE_STALE_MS / 2),
+};
+
 const DEFAULT_OPTIONS = {
   retry: { retries: 10000, interval: 5 },
   read: { encoding: 'utf8' },
@@ -107,7 +115,7 @@ class ExclusiveLockfile {
     this._ensureFileExists();
 
     await retry(this._options.retry, () => {
-      const operationResult = plockfile.lockSync(this._lockFilePath);
+      const operationResult = plockfile.lockSync(this._lockFilePath, LOCKFILE_OPTIONS);
 
       this._isLocked = true;
       this._invalidate();
