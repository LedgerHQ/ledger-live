diff --git a/dist/runtime.cjs b/dist/runtime.cjs
index 156210ecd2f80501bce7a92f0eb85df945030350..39471414dbdc18b6cb221228cd85f97069a1dbef 100644
--- a/dist/runtime.cjs
+++ b/dist/runtime.cjs
@@ -26,7 +26,9 @@ const composeWithRozeniteDevTools = (options = {}) => {
     port: REDUX_DEVTOOLS_PORT,
     secure: false,
     realtime: true,
-    maxAge: options.maxAge ?? 50
+    maxAge: options.maxAge ?? 50,
+    actionSanitizer: options.actionSanitizer,
+    stateSanitizer: options.stateSanitizer
   });
 };
 const rozeniteDevToolsEnhancer = (options = {}) => {
@@ -36,7 +38,9 @@ const rozeniteDevToolsEnhancer = (options = {}) => {
     port: REDUX_DEVTOOLS_PORT,
     secure: false,
     realtime: true,
-    maxAge: options.maxAge ?? 50
+    maxAge: options.maxAge ?? 50,
+    actionSanitizer: options.actionSanitizer,
+    stateSanitizer: options.stateSanitizer
   });
 };
 exports.composeWithRozeniteDevTools = composeWithRozeniteDevTools;
diff --git a/dist/runtime.js b/dist/runtime.js
index 6d2926b277c979c4d7c981c9c90d19fdaaa88ee9..6228640ca61a0ecc63801bded1a382a6c326a2e8 100644
--- a/dist/runtime.js
+++ b/dist/runtime.js
@@ -22,7 +22,9 @@ const composeWithRozeniteDevTools = (options = {}) => {
     port: REDUX_DEVTOOLS_PORT,
     secure: false,
     realtime: true,
-    maxAge: options.maxAge ?? 50
+    maxAge: options.maxAge ?? 50,
+    actionSanitizer: options.actionSanitizer,
+    stateSanitizer: options.stateSanitizer
   });
 };
 const rozeniteDevToolsEnhancer = (options = {}) => {
@@ -32,7 +34,9 @@ const rozeniteDevToolsEnhancer = (options = {}) => {
     port: REDUX_DEVTOOLS_PORT,
     secure: false,
     realtime: true,
-    maxAge: options.maxAge ?? 50
+    maxAge: options.maxAge ?? 50,
+    actionSanitizer: options.actionSanitizer,
+    stateSanitizer: options.stateSanitizer
   });
 };
 export {
diff --git a/dist/src/runtime.d.ts b/dist/src/runtime.d.ts
index 19ff03cfbad1b4eb66554bd838e6503d81f3e590..aae5569b45737d76a7dd9f49d53cbc0d5d756092 100644
--- a/dist/src/runtime.d.ts
+++ b/dist/src/runtime.d.ts
@@ -1,3 +1,4 @@
+import { Action } from 'redux';
 import { devToolsEnhancer, composeWithDevTools } from '@redux-devtools/remote';
 type StoreEnhancer = ReturnType<typeof devToolsEnhancer>;
 type ComposeWithDevTools = ReturnType<typeof composeWithDevTools>;
@@ -13,6 +14,13 @@ export interface RozeniteDevToolsOptions {
      * @default 50
      */
     maxAge?: number;
+    /**
+     * Rewrites every action before it is relayed to the DevTools panel. Use it to remove a
+     * credential an action carries.
+     */
+    actionSanitizer?: (<A extends Action<string>>(action: A, id?: number) => A) | undefined;
+    /** Rewrites the state before it is relayed to the DevTools panel. */
+    stateSanitizer?: (<S>(state: S, index?: number) => S) | undefined;
 }
 export declare const composeWithRozeniteDevTools: (options?: RozeniteDevToolsOptions) => ComposeWithDevTools;
 export declare const rozeniteDevToolsEnhancer: (options?: RozeniteDevToolsOptions) => StoreEnhancer;
