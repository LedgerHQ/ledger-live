mode: ignore
reason: >
  Verify the ignore-gate absorbs the deliberate QUARANTINE-VERIFY failure
  (desktop playwright). Temporary — paired with the forced-failure edit; remove both.
owner: "@LedgerHQ/wallet-ci"
expiry: "2026-12-31"
filter:
  file: "apps/ledger-live-desktop/tests/specs/send.spec.ts"
  # titlePattern (not exact title): Playwright's resolved title is file-prefixed,
  # and "Blacklisted addresses" also exists in receive.spec.ts — the file filter
  # above scopes this entry to send.spec.ts only.
  titlePattern: "Blacklisted addresses"
