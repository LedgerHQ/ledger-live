appId: ${APP_ID}
tags:
  - swap
  - sol
  - eth
---
- runFlow:
    file: ../../subflows/swap.yaml
    env:
      FROM_TICKER: "SOL"
      FROM_NETWORK: "solana"
      FROM_NETWORK_REQUIRED: "false"
      TO_TICKER: "ETH"
      TO_NETWORK: "ethereum"
      TO_NETWORK_REQUIRED: "false"
