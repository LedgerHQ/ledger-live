appId: ${APP_ID}
tags:
  - swap
  - eth
  - usdt
---
- runFlow:
    file: ../../subflows/swap.yaml
    env:
      FROM_TICKER: "ETH"
      FROM_NETWORK: "ethereum"
      FROM_NETWORK_REQUIRED: "false"
      TO_TICKER: "USDT"
      TO_NETWORK: "ethereum"
      TO_NETWORK_REQUIRED: "true"
