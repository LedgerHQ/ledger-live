appId: ${APP_ID}
tags:
  - swap
  - xrp
  - btc
---
- runFlow:
    file: ../../subflows/swap.yaml
    env:
      FROM_TICKER: "XRP"
      FROM_NETWORK: "ripple"
      FROM_NETWORK_REQUIRED: "false"
      TO_TICKER: "BTC"
      TO_NETWORK: "bitcoin"
      TO_NETWORK_REQUIRED: "false"
