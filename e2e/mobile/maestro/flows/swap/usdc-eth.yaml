appId: ${APP_ID}
tags:
  - swap
  - usdc
  - eth
---
- runFlow:
    file: ../../subflows/swap.yaml
    env:
      FROM_TICKER: "USDC"
      FROM_NETWORK: "ethereum"
      FROM_NETWORK_REQUIRED: "true"
      TO_TICKER: "ETH"
      TO_NETWORK: "ethereum"
      TO_NETWORK_REQUIRED: "false"
