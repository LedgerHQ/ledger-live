---
name: package-public-api
description: A new-architecture package (shared/, domain/, features/) exposes its API through barrels that contain nothing but `export *`, and keeps its private code in an internals location. Read this when creating a package under shared/, domain/ or features/, when editing any `index.*` file, or when a `lint:structure` check fails.
---

Read and follow the [canonical skill](../../../.agents/skills/package-public-api/SKILL.md) completely before acting. Treat it as the single source of truth.
